[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    slow: интеграционные тесты, требуют реальный Postgres на localhost:54321 (запускать через `pytest -m slow` или `RUN_SLOW=1`)
